import React from 'react';
import { Typography, Box } from '@mui/material';

const UpdateSubject = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4" sx={{ mb: 2 }}>Withdraw Student</Typography>
      <Typography>This page will be used to withdraw a student from the system.</Typography>
    </Box>
  );
};

export default UpdateSubject;
