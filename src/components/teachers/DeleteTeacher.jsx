import React from 'react';
import { Typography, Box } from '@mui/material';

const DeleteTeacher = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4" sx={{ mb: 2 }}>Withdraw Student</Typography>
      <Typography>This page will be used to Delete Teacher from the system.</Typography>
    </Box>
  );
};

export default DeleteTeacher;
